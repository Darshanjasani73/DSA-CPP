#include<iostream>
using namespace std;
void insertionSort(int arr[],int n)
{
    int arr2[n];
    arr2[0]=arr[0];
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
           if(arr)
        }
    }
}
void printarray(int arr[],int n)
{
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<endl;
    }
}
int main()
{
    int n;
    cout<<"num: "<<endl;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    cout<<endl;
    printarray(arr,n);
    cout<<endl;
    return 0;
}
